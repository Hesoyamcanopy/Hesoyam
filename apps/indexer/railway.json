{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --no-audit --no-fund && npm run contracts:compile && npm run generate -w @hesoyam/sdk"
  },
  "deploy": {
    "startCommand": "npm run start -w @hesoyam/indexer",
    "restartPolicyType": "ALWAYS",
    "numReplicas": 1
  }
}
